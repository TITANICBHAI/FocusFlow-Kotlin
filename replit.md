# Repository navigation

- Treat `__ARCHIVED_DO_NOT_INSPECT__` as a user-archived React app.
- Do not list, search, read, modify, or build files beneath that directory unless the user explicitly asks to work on the archived React app.
- For normal FocusFlow work, use the Kotlin Android project in the repository root and `app/`.