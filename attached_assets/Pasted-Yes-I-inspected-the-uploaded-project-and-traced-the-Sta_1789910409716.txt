Yes. I inspected the uploaded project and traced the **Stats screen dependency graph**. One important finding first: the uploaded `app.zip` is the older React Native/Expo implementation with Kotlin native modules—not the standalone Kotlin UI app you are referring to. So for your current Kotlin app, the right approach is to have the agent locate the equivalent files while using this dependency map as the reference.

### Stats-related files I found in the uploaded project

**Primary Stats screen**

```text
app/(tabs)/stats.tsx
```

This is the central screen. It contains:

* Yesterday / Today / Week / All Time tabs
* Focus-time hero
* Task completion statistics
* Weekly productivity
* Focus-time charts
* All-time statistics
* Streaks
* Temptation/distraction statistics
* Android Usage Access insights
* App usage list
* Quick-block actions
* Heatmap
* Insights panel

**Usage statistics UI**

```text
src/components/UsageInsights.tsx
```

This is particularly important because it is the UI layer that talks to Android's actual device usage data.

**Usage JS/native bridge**

```text
src/native-modules/UsageStatsModule.ts
```

**Actual Android Kotlin implementation**

```text
android-native/app/src/main/java/com/tbtechs/focusflow/modules/UsageStatsModule.kt
```

This is the important Kotlin file for **Android UsageStatsManager**.

It handles things such as:

* Usage Access permission
* Querying `UsageStatsManager`
* Foreground application time
* Per-app usage
* Launch counts
* Last-used timestamps
* Total foreground minutes
* Opening Android Usage Access settings

**Temptation/distraction statistics**

```text
src/native-modules/GreyoutModule.ts
android-native/app/src/main/java/com/tbtechs/focusflow/modules/GreyoutModule.kt
android-native/app/src/main/java/com/tbtechs/focusflow/services/TemptationLogManager.kt
```

The Stats screen uses this to obtain the app-opening/distraction log.

**Weekly insight component**

```text
src/components/InsightsPanel.tsx
```

**Quick block from Usage stats**

```text
src/components/QuickBlockSheet.tsx
```

**Stats database functions**

```text
src/data/database.ts
```

Specifically:

```text
dbGetTodayFocusMinutes()
dbGetTodayOverrideCount()
dbGetStreak()
dbRecordDayCompletion()
dbGetRecentDayCompletions()
dbGetAllTimeFocusMinutes()
dbGetAllTimeFocusSessions()
dbGetBestStreak()
dbGetTasksInDateRange()
```

**Supporting models**

```text
src/data/types.ts
```

Especially:

```text
Task
FocusSession
DailyAllowanceEntry
```

**Week calculation**

```text
src/utils/weekUtils.ts
```

---

## For your actual Kotlin app

Give your agent this instruction. This is more useful than telling it specific old filenames, because the current implementation may have completely different Kotlin/Compose architecture.

```text
I need you to locate the complete Stats screen implementation and every dependency that is actually required by it in the CURRENT Kotlin Android app.

CRITICAL PROJECT BOUNDARY:
- There is a sibling folder named `ARCHIVED_DO_NOT_INSPECT`.
- DO NOT inspect it.
- DO NOT search it.
- DO NOT read files from it.
- DO NOT compare against it.
- DO NOT copy code/assets from it.
- Treat it as completely off-limits.
- Only inspect the active/current app directory.

TASK:

Find the current Stats screen and trace its COMPLETE dependency graph.

Start by locating:
1. The Stats screen / StatsActivity / StatsFragment / StatsComposable.
2. The navigation route that opens Stats.
3. The ViewModel/state holder used by Stats.
4. Every repository/use-case/service queried by Stats.
5. Every database/Room DAO/entity/query required by Stats.
6. Every UsageStatsManager-related Kotlin class.
7. Every class responsible for Android Usage Access permission.
8. Every class responsible for app usage / screen-time aggregation.
9. Every class responsible for focus-session statistics.
10. Every class responsible for task completion statistics.
11. Every class responsible for streak calculations.
12. Every class responsible for weekly statistics.
13. Every class responsible for all-time statistics.
14. Every class responsible for distraction / temptation / blocked-app statistics.
15. Every model/data class used by Stats.
16. Every chart/graph/heatmap component used by Stats.
17. Every resource/drawable/string/color/dimension specifically required by Stats.
18. Any background service that writes or maintains the usage/statistics data consumed by Stats.

Do NOT merely find filenames containing "Stats".

Trace the actual imports, function calls, ViewModel dependencies, repository dependencies, DAO queries, and data flow.

I specifically need to understand:

DATA FLOW

Android UsageStatsManager
        ↓
usage statistics repository/service
        ↓
ViewModel/state
        ↓
Stats screen

and separately:

Task/session database
        ↓
repository/DAO
        ↓
statistics calculations
        ↓
Stats ViewModel
        ↓
Stats screen

and, if present:

Accessibility / blocking / temptation events
        ↓
event/log storage
        ↓
statistics aggregation
        ↓
Stats screen

For every discovered file, classify it as one of:

A. CORE STATS SCREEN
B. DIRECT STATS DEPENDENCY
C. USAGE-STATS DEPENDENCY
D. DATABASE/ROOM DEPENDENCY
E. DISTRACTION/TEMPTATION DEPENDENCY
F. SHARED MODEL
G. NAVIGATION DEPENDENCY
H. UI/CHART RESOURCE
I. BACKGROUND DATA PRODUCER
J. UNRELATED — found during search but not actually required

For each relevant file give:

- exact relative path
- Kotlin class/object/function
- why Stats needs it
- what data it provides
- who calls it
- whether it is UI, domain, data, or Android-native infrastructure

IMPORTANT:
Do not modify anything yet.

First produce a dependency map.

Then identify the MINIMUM set of files that would need to be copied/reimplemented if I wanted to reproduce the existing Stats functionality in another Kotlin app.

Also explicitly distinguish:
- files required for the Stats UI
- files required for calculating statistics
- files required for Android Usage Access
- files required for collecting/storing the underlying data
- files that are merely shared with other features

Do not inspect `ARCHIVED_DO_NOT_INSPECT` under any circumstances.
```

### One especially important point

For the **usage portion** of Stats, don't let the agent assume that "Stats" means only database statistics.

The existing architecture has two fundamentally different data sources:

```text
FOCUSFLOW INTERNAL DATA
Tasks
Focus sessions
Completion
Streaks
Day completions
        ↓
      Stats


ANDROID DEVICE DATA
UsageStatsManager
        ↓
Foreground time
Launch count
Last used
Per-app usage
        ↓
   Usage Insights
```

And the old Kotlin implementation confirms that `UsageStatsModule.kt` is doing real Android `UsageStatsManager` work, rather than merely reading FocusFlow's own database.

Also, the Stats screen's distraction section comes from a **third source**:

```text
Accessibility / blocking events
        ↓
TemptationLogManager
        ↓
GreyoutModule
        ↓
Stats
```

So if your goal is to reproduce the **entire existing Stats screen** in the new Kotlin app, the agent needs to trace all three pipelines rather than just finding a `StatsScreen.kt`.
