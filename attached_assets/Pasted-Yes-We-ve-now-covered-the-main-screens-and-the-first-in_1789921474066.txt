Yes. We’ve now covered the main screens and the first internal screens. The next pass should be the **action/configuration surfaces** that appear from Defense, Focus, Launcher, and Always-On.

I inspected the latest Kotlin ZIP and the React implementation again. The important finding is that many of these Kotlin components are **functionally rich but visually much heavier than the React versions**. The biggest offenders are `ModalBottomSheet`, `AlertDialog`, oversized rounded buttons, and large vertical padding.

So this pass should be very explicit.

# Phase 6 — Defense / Launcher / Focus action surfaces

Send this to the agent:

```text id="8t7p6e"
FOCUSFLOW UI PHASE 6
DEFENSE / LAUNCHER / FOCUS ACTION SURFACES

The main screens and internal screens have already been handled.

This phase covers only the secondary action/configuration surfaces:

DEFENSE:
    ui/defense/BlockedWordsModal.kt
    ui/defense/DailyAllowanceDefenseDialog.kt
    ui/defense/GreyoutScheduleModal.kt
    ui/defense/NuclearModeModal.kt
    ui/defense/StandaloneBlockModal.kt

LAUNCHER:
    ui/launcher/AppPickerSheet.kt
    ui/launcher/QuickBlockSheet.kt
    ui/launcher/AllowedAppsModal.kt

ALWAYS-ON:
    ui/alwayson/VpnConsentModal.kt
    ui/alwayson/VpnPermissionLostBanner.kt

FOCUS:
    ui/focus/ExtendModal.kt
    ui/focus/SessionDebriefModal.kt

DO NOT modify business logic.
DO NOT modify ViewModels/repositories.
DO NOT modify persistence.
DO NOT modify navigation routes.

The React implementation is the sizing and geometry source of truth.

============================================================
GLOBAL RULE
============================================================

Do not make these screens larger than the React implementation.

Avoid:

    18–24dp arbitrary vertical padding
    huge buttons
    giant text fields
    stock Material AlertDialog geometry
    giant rounded corners
    Material Switch
    oversized app rows

Target family:

    primary text       15sp
    secondary text     11–13sp
    section label      11sp
    icon               16–22dp
    app icon            40dp
    input               42–48dp
    action button       44–48dp
    compact action      34–40dp
    radius              10–16dp
    modal radius        20–24dp
    card padding        12dp
    screen padding      16dp

Use the previously-created shared:

    FocusFlowInternalHeader
    FocusFlowModalCard
    FocusFlowPrimaryButton
    FocusFlowSecondaryButton
    FocusFlowModalField
    FocusFlowSwitch
    FocusFlowInternalCard

where appropriate.

============================================================
1. BLOCKED WORDS MODAL
============================================================

File:

    ui/defense/BlockedWordsModal.kt

The React implementation is a full-screen page-style editor.

KEEP:
    Dialog / full-screen behavior

Do NOT convert it back to ModalBottomSheet.

Current Kotlin uses oversized elements like:
    17sp title
    14dp field radius
    large Material OutlinedTextField
    large Button

Adjust to:

HEADER:

    horizontal padding = 16dp
    vertical padding = 10dp
    bottom divider = 1dp

    Cancel:
        13sp

    center icon:
        15–16dp

    title:
        15sp
        bold

    Save:
        13sp
        bold

Do not use a 17–18sp modal title.

LOCK / DEFENSE / INFO BANNERS:

    horizontal = 16dp
    vertical = 8–10dp
    icon = 14dp
    text = 11sp
    lineHeight ~= 17dp

Do not give these 16–20dp corner radii.
Prefer:
    10dp

WORD INPUT:

Use shared FocusFlowModalField.

Target:

    height ~= 44dp
    radius = 10dp
    text = 13sp
    horizontal padding ~= 12dp

Add button:

    44dp x 44dp
    radius = 10dp
    icon = 20dp

Do not use a full-height oversized Material Button.

KEYWORD CHIPS:

    full pill
    horizontal = 12dp
    vertical = 6dp
    text = 13sp
    delete icon = 15–17dp

EMPTY STATE:

    icon ~= 40dp
    text = 15sp
    supporting = 11sp

CLEAR ACTION:

    compact text action
    13sp

Keep:
    lock semantics
    defense PIN behavior
    duplicate prevention
    save behavior
    remove behavior

============================================================
2. DAILY ALLOWANCE
============================================================

File:

    ui/defense/DailyAllowanceDefenseDialog.kt

This is only a coordinator/dialog wrapper.
Do not inflate it with new UI.
Any actual presentation used by the full Daily Allowance flow should follow
the same compact geometry as StandaloneBlockModal.

If the implementation redirects into the Daily Allowance UI:

    keep navigation/state
    only refine the destination component

The important dimensions are:

    app icon = 40dp
    app title = 15sp
    package = 11sp
    row padding = 12dp horizontal / 8dp vertical
    search = 42–44dp
    checkbox/action box = 28–32dp

Do not make the daily allowance controls into large cards.

============================================================
3. GREYOUT SCHEDULE MODAL
============================================================

File:

    ui/defense/GreyoutScheduleModal.kt

React implementation is a page-style modal with a compact header and
a nested "add/edit window" mode.

CURRENT KOTLIN:
    ModalBottomSheet

Replace the OUTER modal with a full-screen Dialog/page surface.

Do not leave the outer container as a bottom sheet.

Header:

    padding horizontal = 16dp
    padding vertical = 10–12dp
    title = 18sp
    back/close icon = 22dp

WINDOW LIST MODE:

Each saved window:

    radius = 10dp
    padding = 12dp
    package/app title = 15sp
    time = 18sp
    days = 11sp
    small count badge = 11sp

Add Button:

    height ~= 40–44dp
    radius = 10dp
    icon = 18dp
    text = 13sp

Save:

    height ~= 40–44dp
    radius = 10dp
    text = 13sp

ADD/EDIT WINDOW MODE:

Section labels:

    11sp
    uppercase
    semibold
    letterSpacing ~= 0.8sp

App search:

    height = 42dp
    radius = 10dp
    icon = 16dp
    text = 13sp

App result row:

    icon = 32dp
    title = 13sp
    package = 11sp
    row vertical padding ~= 8dp

Selected app chips:

    compact full pills
    text = 11sp
    close icon = 14–16dp

TIME CONTROLS:

Use compact stepper buttons.

Each step button:
    28dp x 28dp
    radius = 6dp
    icon = 14dp

Displayed time:
    15sp
    semibold

Day selector:
    compact 32–36dp controls
    selected = BrandPrimary
    text ~= 11sp

VPN switch:

    MUST use shared FocusFlowSwitch

Do not use the Material Switch that currently appears inside this modal.

Existing:
    defense PIN
    app selection
    schedule validation
    VPN toggle
    save/edit/delete

all remain unchanged.

============================================================
4. NUCLEAR MODE
============================================================

File:

    ui/defense/NuclearModeModal.kt

React implementation is a page-style modal.

CURRENT KOTLIN:
    ModalBottomSheet

Change outer presentation to full-screen Dialog.

Header:

    horizontal = 12–16dp
    vertical = 12dp
    title = 15sp
    close = 40dp touch target
    icon ~= 22dp

Warning banner:

    radius = 10dp
    padding = 12dp
    warning icon ~= 20dp
    title = 15sp
    body = 15sp max
    lineHeight ~= 18–20dp

Important:
Do not make warning body 18–20sp.
15sp maximum.

SECTION LABEL:

    11sp uppercase
    muted
    letterSpacing ~= 0.8sp

APP ROW:

    radius = 10dp
    border = 1dp
    padding = 12dp
    app icon = 40dp
    app title = 15sp
    package = 11sp

UNINSTALL:

    compact red button
    min width ~= 80dp
    vertical padding ~= 6dp
    text = 15sp

EMPTY:

    radius = 16dp
    padding = 24dp
    title = 15sp
    body = 13sp

TIP:

    radius = 10dp
    padding = 12dp
    icon ~= 18dp
    body = 13sp

Do not make Nuclear Mode visually overwhelming just because it is a
serious feature.

Keep all uninstall/security semantics.

============================================================
5. STANDALONE BLOCK
============================================================

File:

    ui/defense/StandaloneBlockModal.kt

The current Kotlin implementation is already full-screen.

KEEP that architecture.

Do NOT convert it to BottomSheet.

The main issue is visual density.

HEADER:

    horizontal = 16dp
    vertical = 12dp

    Cancel = 15sp
    title = 15sp
    Save = 15sp

EXPIRY SECTION:

    horizontal = 16dp
    vertical = 12dp

    label:
        11sp uppercase

    controls:
        compact
        40–44dp high
        radius = 10dp
        text = 13sp
        icon = 18dp

WARNING / ERROR / INFO BANNERS:

    radius = 12dp
    padding = 10–12dp
    icon = 18–20dp
    text = 12sp

PRESET CONTROLS:

    preset pills:
        compact
        12–13sp

    preset action:
        compact 40–44dp

APP SEARCH:

    search field = 42dp
    radius = 10dp
    icon = 16dp
    input = 13sp

APP ROWS:

    horizontal padding = 16dp
    vertical = 8dp
    icon = 40dp
    radius = 6dp
    title = 15sp
    package = 11sp

DAILY ALLOWANCE CONFIG:

    compact card
    radius = 10dp

    mode buttons:
        32–36dp high
        text = 11sp

    +/- buttons:
        28dp
        radius = 6dp
        icon = 14dp

    values:
        13sp

VPN TOGGLE:

    shared FocusFlowSwitch.

BOTTOM SAVE:

    compact 44–48dp
    radius = 10dp
    text = 15sp

Do NOT use:
    56dp controls
    18sp secondary text
    20dp+ radii for every subcard

Keep all block/preset/recurring schedule logic unchanged.

============================================================
6. APP PICKER SHEET
============================================================

File:

    ui/launcher/AppPickerSheet.kt

React implementation is a full page-style picker.

CURRENT KOTLIN:
    ModalBottomSheet with top radius 30dp

Replace the outer presentation with a full-screen Dialog/page surface.

Header:

    padding horizontal = 16dp
    vertical = 12dp
    title = 15sp
    Close touch target = 40dp
    close icon = 18dp

PRESETS SECTION:

    label = 11sp uppercase
    preset chips:
        horizontal = 12dp
        vertical = 6dp
        text = 13sp
        full pill

Select All / Deselect All:

    compact
    36–40dp high
    12–13sp

Search:

    height = 42dp
    radius = 16dp
    horizontal padding = 12dp
    icon = 16dp
    text = 15sp

APP ROW:

    radius = 10dp
    padding horizontal = 12dp
    vertical = 8dp
    icon = 40dp
    icon radius = 6dp
    title = 15sp
    package = 11sp

SENSITIVE badge:

    9sp
    tiny pill
    compact padding

CHECKBOX:

    22dp
    radius = 6dp
    border = 2dp
    selected = BrandPrimary

Do NOT use oversized checkboxes or Material Checkbox geometry.

BOTTOM SAVE:

    compact 44–48dp
    radius = 10dp
    text = 13sp

Preset creation input:

    height ~= 42dp
    radius = 10dp
    text = 13sp

Keep:
    selection semantics
    presets
    save
    delete preset
    sensitive-app warnings
    noneWhenEmpty behavior

unchanged.

============================================================
7. QUICK BLOCK SHEET
============================================================

File:

    ui/launcher/QuickBlockSheet.kt

React implementation is intentionally compact.

CURRENT KOTLIN:
    large ModalBottomSheet
    20sp title
    14sp subtitle
    14dp rounded buttons

Keep it a bottom sheet because the React reference actually uses a sheet.

BUT reduce its geometry.

Sheet:

    top corners = 22dp
    horizontal padding = 16dp
    bottom padding = safe inset + 16dp

Handle:

    42 x 4dp
    radius = 2dp

App icon:

    48dp
    radius = 14dp
    inner icon = 24dp

Title:

    15sp
    bold

App subtitle:

    11sp

Section label:

    11sp
    uppercase
    letterSpacing ~= 0.8sp

TEMPORARY BLOCK options:

    grid = 2 columns

    option:
        minHeight = 52dp
        radius = 10dp
        padding ~= 8dp
        icon = 20dp
        label = 13sp

ALWAYS-ON button:

    radius = 10dp
    padding = 8dp
    icon = 22dp
    title = 13sp
    supporting = 11sp

MANAGE card:

    radius = 10dp
    padding = 8dp
    title = 13sp
    supporting = 11sp

Manage buttons:

    minHeight = 36dp
    radius = 6dp
    icon = 16dp
    text = 11sp

Privacy note:

    11sp
    centered
    lineHeight ~= 17dp

Do not make the "Block for one hour" button 20sp/large.

Keep all temporary blocking logic.

============================================================
8. VPN CONSENT
============================================================

File:

    ui/alwayson/VpnConsentModal.kt

Current Kotlin uses AlertDialog.

Replace with a custom Dialog using FocusFlowModalCard.

React target:

    modal card radius = 24dp
    padding = 24dp
    icon circle = 64dp
    icon = 32dp
    title = 18sp
    body = 14sp
    facts = 14sp
    action buttons ~= 44–48dp
    buttons radius = 10dp

Backdrop:

    black ~= 0.60 alpha

Structure:

    64dp icon circle
    ↓ 16dp
    title
    ↓ 12dp
    description
    ↓ 12dp
    fact rows
    ↓ 12dp
    info block
    ↓ 16dp
    primary action
    ↓ 8dp
    cancel

Do NOT use AlertDialog.

Keep the Android VPN-consent callback exactly unchanged.

============================================================
9. VPN PERMISSION LOST BANNER
============================================================

File:

    ui/alwayson/VpnPermissionLostBanner.kt

React reference:

    card radius = 16dp
    horizontal padding = 12dp
    vertical padding = 10dp
    icon container = 36dp
    icon ~= 20dp
    title = 13sp
    body = 11sp
    button = compact

Replace the current Material Card/Button presentation.

Use:

FocusFlowInternalCard(radius = 16.dp, contentPadding = 12.dp)

Icon container:
    36dp
    radius = 18dp

Title:
    13sp
    semibold

Description:
    11sp

Restore VPN:
    height ~= 36dp
    radius = 10dp
    icon = 14dp
    text = 12sp

Do NOT make the banner 56–64dp tall unless its content actually needs it.

Keep the existing restore action.

============================================================
10. FOCUS EXTEND
============================================================

File:

    ui/focus/ExtendModal.kt

React implementation:

    bottom sheet
    radius top = 24
    padding = 24
    title = 22sp
    subtitle = 13sp
    duration options = compact cards

Keep bottom-sheet architecture.

Header:

    icon container = 40dp
    icon = 22dp
    title = 18sp
    supporting = 13sp

Duration options:

    radius = 10dp
    padding vertical ~= 12dp
    text = 15sp

Do not make each choice button 56dp+ high.

Cancel:
    compact
    13–15sp

Use the same visual family as the task duration controls.

Keep extend callback and timing logic.

============================================================
11. SESSION DEBRIEF
============================================================

File:

    ui/focus/SessionDebriefModal.kt

Current Kotlin uses AlertDialog.

React debrief is a compact result modal.

Replace AlertDialog with custom Dialog / FocusFlowModalCard.

Target:

    radius ~= 20dp
    padding = 24dp

Header:
    icon / status ~= 40–48dp
    title = 18sp
    close = 40dp hit area
    icon = 22dp

"SESSION DEBRIEF":
    11sp
    uppercase
    letterSpacing ~= 0.8sp

Primary result:
    22sp

Supporting:
    13sp
    lineHeight ~= 20dp

Stats:
    compact rows/cards
    13–15sp

Done:
    height ~= 48dp
    radius = 10dp
    text = 15sp

Keep all session summary values and dismissal behavior unchanged.

============================================================
12. IMPORTANT: DO NOT DUPLICATE COMPONENTS
============================================================

If a component now exists:

    FocusFlowPrimaryButton
    FocusFlowSecondaryButton
    FocusFlowModalField
    FocusFlowModalCard
    FocusFlowSwitch
    FocusFlowInternalCard

reuse it.

Do NOT introduce:

    Another custom switch
    Another custom primary button
    Another app row implementation
    Another modal-card implementation

unless the geometry is genuinely different.

============================================================
13. MATERIAL COMPONENTS TO REMOVE IN THIS PASS
============================================================

Replace where they materially differ from React:

    AlertDialog
    ModalBottomSheet
    Button
    OutlinedButton
    Switch
    Checkbox
    OutlinedTextField
    Material3 Card

Allowed exception:

    Keep ModalBottomSheet for QuickBlockSheet / ExtendModal where the
    React implementation is actually a bottom sheet.

Everything else should use the FocusFlow primitives or explicit Compose
layout.

============================================================
14. BUSINESS LOGIC PROTECTION
============================================================

Absolutely do not change:

    PIN verification
    PIN requirements
    locked keyword behavior
    daily allowance rules
    app selection semantics
    VPN consent
    VPN restoration
    nuclear mode uninstall behavior
    recurring schedules
    standalone block expiry
    temporary blocking
    preset persistence
    focus extension
    session debrief calculations

Only replace the presentation layer.

============================================================
15. STOP CONDITION
============================================================

After this pass, build and stop.

Do NOT continue into:
    diagnostics
    support
    profile
    legal
    onboarding

Those are already covered by the previous internal-screen instructions.
```

## One specific thing I want the agent to preserve

For `StandaloneBlockModal.kt`, **do not simplify the huge screen into a generic form**. That file contains a lot of actual product behavior:

```text
temporary block
daily allowance
VPN package selection
presets
recurring schedules
installed-app search
PIN-protected removal
expiry
```

The React version also has all of that. The correct transformation is:

```text
same functionality
        ↓
same information architecture
        ↓
much tighter geometry
```

not:

```text
complex product screen
        ↓
generic simplified form
```

That distinction is important because earlier attempts at this app tended to "clean up" screens by accidentally removing the density and semantics that make the product useful.

### The next pass after this one

Once this phase is done, the remaining meaningful surfaces are mostly:

```text
Focus:
    ActiveBlockScreen
    SessionDebrief / Extend refinements

Support:
    Diagnostics
    Troubleshoot
    Report Issue
    How to Use
    Changelog

Profile/security:
    Permissions
    User Profile
    Password Protection
    PIN flows

Settings internals:
    Daily Allowance Modal
    Overlay Appearance Modal
    Dark Mode
    notifications-related dialogs

Data/legal:
    Backup Import
    Privacy
    Terms

Startup:
    Onboarding
```

Then the UI work is essentially a **consistency pass** rather than another redesign.
